eureka:
  client:
    service-url:
      defaultZone: http://hupeng:123456@${eureka.instance.hostname}:8000/eureka/
  instance:
    hostname: localhost
spring:
  cloud:
    config:
      discovery:
        service-id: basic-config-server
        enabled: true


